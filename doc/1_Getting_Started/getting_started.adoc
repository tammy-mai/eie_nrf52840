= Getting started with EIE
:doctype: article
:imagesdir: imgs
:toc:
:source-highlighter: highlight.js
:sectnums:

== Introduction

This module describes the software installation required for the EIE Program. All software is freeware or Free Open Source Software.

There are different install instructions for Windows, MacOS and Linux. Follow all instructions _Carefully_ to make sure your setup works.

== OS Specific Installation

The following section documents specific instructions for installations for Windows, MacOS and Linux.
Follow the relevant instructions for your system.

=== Windows

.Click to expand
[%collapsible]
====
1. Open the windows terminal application. If you do not have windows terminal installed get it from the https://apps.microsoft.com/detail/9n0dx20hk701[microsoft store]
2. Verify you have winget installed by running the command `winget --help` in the terminal. If installed help text for the Windows Package Manager should show up. If not installed install https://aka.ms/getwinget[winget]
3. Use Winget to install dependencies `winget install Kitware.CMake Ninja-build.Ninja oss-winget.gperf python Git.Git oss-winget.dtc wget 7zip.7zip nrfutil astral-sh.uv`.
   a. You may need to add 7zip to the path
   b. type 'python --version' which should say a number >13.0
4. Download the latest https://www.segger.com/downloads/jlink/[jLink installer]
   a. start the setup process
   b. make sure to check the "Install Legacy USB driver" checkbox
   c. finish the setup process
5. Open Git bash in the windows terminal
6. Run `nrfutil install device`
====

=== MacOS

.Click to expand
[%collapsible]
====
1. Open a terminal
2. Install homebrew by running `/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"`
3. Add the home brew installation to the path. If you have a Apple silicon mac (2020 or newer) run `(echo; echo 'eval "$(/opt/homebrew/bin/brew shellenv)"') >> ~/.zprofile && source ~/.zprofile`. If you have an intel mac (2020 or older) run `(echo; echo 'eval "$(/usr/local/bin/brew shellenv)"') >> ~/.zprofile && source ~/.zprofile`
4. Use homebrew to install the required dependencies by running `brew install cmake ninja gperf python3 python-tk ccache qemu dtc libmagic wget openocd`
5. Add python to the path `(echo; echo 'export PATH="'$(brew --prefix)'/opt/python/libexec/bin:$PATH"') >> ~/.zprofile && source ~/.zprofile`
6. Download the latest https://www.segger.com/downloads/jlink/[jLink installer] and run through the setup process.
7. Download https://www.nordicsemi.com/Products/Development-tools/nRF-Util[nRF utils]
8. Change the directory in your terminal to Downloads `cd ~/Downloads`
9. Make nrfutils executable `chmod +x nrfutil`
10. Move nrfutils to your local bin `mv nrfutil ~/.local/bin/`. If this folder does not exist create it `mkdir -p ~/.local/bin` and add it to your path `echo; echo 'export PATH=$PATH:$HOME/.local/bin' >> ~/.zprofile && source ~/.zprofile`.
11. Open a new terminal
12. Run `nrfutil install device`
13. Install Astral's UV tool `curl -LsSf https://astral.sh/uv/install.sh | sh`
====

=== Linux

.Click to expand
[%collapsible]
====
NOTE: This assumes Ubuntu 24.04 but you should be able to adapt this for any linux distribution

1. Run `sudo apt install --no-install-recommends git cmake ninja-build gperf ccache dfu-util device-tree-compiler wget python3-dev python3-venv python3-tk xz-utils file make gcc gcc-multilib g++-multilib libsdl2-dev libmagic1`
2. Download the latest https://www.segger.com/downloads/jlink/[jLink installer] and run through the setup process.
3. Download https://www.nordicsemi.com/Products/Development-tools/nRF-Util[nRF utils]
4. Change the directory in your terminal to Downloads `cd ~/Downloads`
5. Make nrfutils executable `chmod +x nrfutil`
6. Move nrfutils to your local bin `mv nrfutil ~/.local/bin/`. If this folder does not exist create it `mkdir -p ~/.local/bin` and add it to your path `echo; echo 'export PATH=$PATH:$HOME/.local/bin' >> ~/.profile && source ~/.profile`.
7. Run `nrfutil install device`
8. Install Astral's UV tool `curl -LsSf https://astral.sh/uv/install.sh | sh`
====

== Github setup

Start with this before proceeding to the OS setup

1. If you don't already have one create a github account. It is strongly recommended to use a professional, but non school email for this and to link your github account on your resume.
2. Go to the https://github.com/eiefirmware/eie_nrf52840[eie_nRF52840] repository
3. Above the about information there is a dropdown that says Fork. Click the dropdown and hit create a new fork. This creates a private copy of the repository for you to use
4. Choose a descriptive name for the repository and hit create fork in the bottom right. You can leave the name as the default eie_nrf52840 if you like

=== Configuring Git

We now need to do a few things to configure Git on your computer.

==== Github Authentication

GitHub disabled password authentication a while ago and instead needs some sort of key for authentication.

. Run `git config --global user.name '<TAMMY_MAI>'` in your terminal
. Run `git config --global user.email '<YOUR_GITHUB_EMAIL>'` in your terminal
. Run `git config credential.helper 'store'` in your terminal
. Follow github's guide to https://docs.github.com/en/authentication/connecting-to-github-with-ssh/generating-a-new-ssh-key-and-adding-it-to-the-ssh-agent#generating-a-new-ssh-key[generating a ssh key].
Note: You shouldn't need to follow the instructions to add an ssh key to the ssh-agent.
. Follow github's guide to https://docs.github.com/en/authentication/connecting-to-github-with-ssh/adding-a-new-ssh-key-to-your-github-account#adding-a-new-ssh-key-to-your-account[add a new SSH key to your account].
. Test that your SSH connection works following https://docs.github.com/en/authentication/connecting-to-github-with-ssh/testing-your-ssh-connection[Github's guide].

==== Git Editor

Git needs an editor to use to write commit messages.
By default Git uses the Vi editor, which if you are not familiar with can be hard to use.
To set your own editor run the following command based on the editor you prefer.

* VScode: `git config --global core.editor "code --wait"`
* Sublime Text: `git config --global core.editor "subl -n -w"`
* Notepad&#43;+: `git config --global core.editor "'C:/Program Files/Notepad+\+/notepad++.exe' -multiInst -notabbar -nosession"`
* Xcode: `git config --global core.editor 'xed -w'`
* nano: `git config --global core.editor nano`

If you have another preferred editor, you can probably set it as the default if desired.

== Configuring the Project

1. Open a new terminal and Change the current directory to your home directory: `cd ~`
2. Create a new directory for zephyr projects: `mkdir zephyr-projects`
3. Change the directory to the zephyr projects directory `cd zephyr-projects`
4. Go to the repo you created during the #github-setup[github setup] and click on the green code button and copy the SSH URL. The URL should look something like `git@github.com:<your account>/eie_nrf52840.git` depending on what you named the repository. **DO NOT DOWNLOAD THE CODE AS A ZIP**
5. Clone the EIE source code `git clone <Your Repo Link Here> eie_nrf52840`
6. Move into the cloned repo `cd eie_nrf52840`
7. Sync the UV project `uv sync`
8. Start the virtual environment `source .venv/bin/activate` or `source .venv/Scripts/activate` on windows
9. Initialize the zephyr project `west init -l`
10. Update west `west update`
11. Run `west zephyr-export`to create necessary files for cmake to run
12. Install the required python packages for west `west packages pip --install`
13. Install the required toolchain `west sdk install --gnu-toolchains arm-zephyr-eabi`
14. Proceed to #building-and-flashing-the-board[Building and Flashing the Board]

=== Virtual Environments

In the previous section we used the uv tool to create a virtual environment.
We use virtual environments to maintain different versions of packages required for different projects.
In this case the virtual environment is used for managing the python packages needed to build the zephyr project.
You can read more about how python virtual environments work https://docs.python.org/3/library/venv.html[here].
Astral's UV project enhances python's virtual environments by also managing python versions.
You can read more about UV https://docs.astral.sh/uv/[here].

Throughout this course we will be using the west tool built by zephyr.
We installed this tool as part of the virtual environment, so to use it we need to make sure our terminal is running the application in the virtual environment.
There is two ways we can do this.

First we can start the virtual environment in the terminal.
To do this we run either `source .venv/bin/activate` on Linux and MacOS or `source .venv/Scripts/activate` on Windows from the folder that contains the virtual environment (In our case the eie_nrf52840 directory).
The image below shows an example of this

image::virtual-env.png[]

In this example at first I am out of the virtual machine and running `west -V` to show the installed west version fails.
After I run the command to activate the virtual environment (notice my terminal is in the project directory set up earlier), the virtual environment has been activated.
We can see the environment is active as the name of the environment, in this case eie-nrf52840, has been pre-pended in parenthesis in front of my terminal prompt.
Once in the virtual environment running `west -V`, and any subsequent west commands, will work as expected.
Please note that the rest of the terminal prompt may look different from what is shown above based on your operating system, or terminal preferences.
Additionally at any point we can exit the virtual environment by running `deactivate`.
If you are using the integrated terminal built into VS Code, it will automatically activate the virtual environment provided you open full project in its own window.

The second way to run tools in the virtual environment is to have uv invoke the virtual environment when we run a command.
To do this we prepend any command with `uv run`.
Using the same example above I can get the west version when not in the virtual environment, simply by telling it to run through uv.

image::uv-run.png[]

In this case however, I would have to prepend `uv run` to all subsequent west commands I want to run.

For the rest of this lesson and all future lessons we assume you have properly activated your virtual environment or are adding `uv run` whenever west is invoked.

== Building and Flashing the Board

1. If you don't already have a terminal open, open a terminal and navigate to the EIE project directory you cloned
2. Activate the virtual environment if not already active (see OS specific instructions for how to do so)
3. Run `west build -b nrf52840dk/nrf52840 app` to build the application
4. Run `west flash` to flash the board. Note if this is your first time flashing you may need to use

== VSCode Setup

Microsoft's Visual Studio Code (VSCode) is the recommended IDE for EIE, however any other will also work.
The following section covers setup for tooling for EIE.
If you are not using VSCode you can skip this section.

. If you don't already have VSCode installed install it from [here](https://code.visualstudio.com/) and run the installer.
. Open the project directory in VSCode.
This can be done by either opening vscode and using `File > Open Folder` and opening the eie_nrf52840 directory we had cloned in the earlier steps or in a terminal navigate to the eie_nrf52840 directory and run `code .`
+
WARNING: Do not just open the files you are editing.
VSCode uses files in the `.vscode`, `.venv`, and `.git` folders in the `eie_nrf52840` directory to help us with editing our project.
If you just open the source files we want to edit without opening the full project VSCode may not be able to debug, or properly inspect certain parts of your project.
+
Once open you should have a view similar to this.
At the very least you should see the a similar directory structure to the one seen on the left.
+
.VSCode on Startup
image::code-startup.png[]

. By default on the far left side of the VSCode window we see what is known as the "activity bar".
+
.The VSCode Activity Bar. Source https://code.visualstudio.com/api/ux-guidelines/activity-bar[microsoft]
image::activity-bar.png[]
+
By default the activity bar contains in order
+
* **Explorer**: This is where you can see and open any files.
It can be quickly accessed with `CTRL+SHIFT+E` (`&#x2318;+SHIFT+E` on mac)
* **Search**: This is a global search area that can be used to search all files in your current open project.
It can be quickly accessed with `CTRL+SHIFT+F` (`&#x2318;+SHIFT+F` on mac).
If you have text selected when you use the quick access shortcut it will enter that text as the search field.
It can also be used to find and replace across your whole project and supports regular expression (regex) search allowing you to search for text matching a pattern.
See https://code.visualstudio.com/docs/editing/codebasics#_search-across-files[Microsoft's Documentation] for more details.
* **Source Control**: This tab allows you to track changes being made to your project.
We will cover it in more detail in link:../2_Version_Control/README.MD[lesson 2] which is on the version control system git.
It can be quickly accessed with `CTRL+SHIFT+G` (`&#x2318;+SHIFT+G` on mac).
* **Run and Debug**: This tab allows you to start a debug session of your project.
It will be covered in more detail in a future lesson.
It can be quickly accessed with `CTRL+SHIFT+D` (`&#x2318;+SHIFT+D` on mac).
* **Extensions**: This tab allows you to install extensions for VSCode that modify and extend its functionality.
It can be quickly accessed with `CTRL+SHIFT+E` (`&#x2318;+SHIFT+E` on mac).
We want to install the recommended extensions for EIE.
If you search `@recommended` in extensions tab the recommended extensions for EIE will come up.
Install all extensions listed under Workspace Recommendations
+
.Search for `@recommended` extensions. Install everything that is listed under Workspace Recommendations. Note this is not a complete list in the image
image::vscode-extensions.png[]

. When prompted to, install clangd.
+
image::clangd_prompt.png[]
+
. Restart VSCode after installing extensions.

At this point VSCode is ready for us to use.

=== Brief intro to VSCode

This section provides a brief intro some useful features of VSCode you may not be familiar with that we will be using in EIE.
This assumes a basic knowledge of how to use VSCode as a text editor.

We will be using the terminology defined by Microsoft for the various elements of the UI.

.VSCode basic layout. Source microsoft
image::code-layout.png[]

=== Explorer

The Explorer is accessed via the activity bar and is viewed in the primary side bar.
It shows a tree view of all files in the currently open project and allows you to open them.
The explorer file tree highlights files you have modified since you last interacted with the version control system.
This will be explored in more detail in the the next lesson on version control.

It also shows an outline and timeline of the currently open file allowing you to easily find what is defined in the file you are looking at and view its history.

.The explorer for the EIE project. We can see the file list at the top with the outline of the open file, in this case LED.c, showing all the C type, variable and function declarations and editing timeline of the file.
image::explorer.png[]

=== Debugger

The debugger allows us to build, flash and step through the execution of our program line by line.
We will cover the debugger in more detail in a future lesson but for now you can start debugging by pressing the green play button in the top.
Make sure the debug configuration selected is "Cortex Debug".
You can also launch the debugger from anywhere by pressing `F5` (Depending on your computer you may either need to enable function lock or use `FN+F5`` to preform this shortcut.)
The debugger can show you the state of variables as you step through the program, the call stack (what functions have been called to get to the point we are at), and the current line being executed.

.The VSCode Debugger
image::debug.png[]

=== The Panel

The panel provides additional information and tools below the editor.
For now the two tools we will look at in the panel are the Problems and Terminal panel.
The panel can be opened or closed using `CTRL+J` (`&#x2318;+J` on mac).

The problems panel shows you a quick list of issues with your code.
For example consider the following code snippet where a variable was defined and then misused.

image::bad_code.png[]

The problems panel highlights exactly where this happened and in this case gives a suggested solution.

.The Problem panel
image::problems_panel.png[]

The terminal panel is a built in terminal in VScode.
It also will automatically start the correct python virtual environment if one is defined in the workspace.
The terminal can be toggled open using `CTRL+\`` (`&#x2318;+`` on mac) or a new terminal can be opened with `CTRL+SHIFT+\`` (`&#x2318;+SHIFT+`` on mac)

.The VScode terminal
image::terminal_panel.png[]

=== Building from VSCode

We define several tasks to build, and flash the nRF52840 dev kit.
We can view all tasks by hitting `CTRL+SHIFT+P` (`&#x2318;+SHIFT+P` on mac) to bring up the command pallet and entering `Tasks: Run Tasks`
This pulls up the list of tasks shown below

image::Tasks.png[]

The 3 tasks we define are:

- **Build**: Build the firmware. Allows specifying a pristine build and the build target.
- **Flash**: Flash the latest build onto the connected dev kit.
- **Build and Flash**: Combines Build and Flash into one step.

Any other tasks listed are not relevant.

The Build and Flash task is also defined as a default build task and can be launched at any time with the shortcut `CTRL+SHIFT+B` (`&#x2318;+SHIFT+B` on mac)

=== Learn More

https://code.visualstudio.com/docs[Microsoft's documentation] provides more resources for learning about the features of VSCode.
